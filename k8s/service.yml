apiVersion: v1
kind: Service
metadata:
  name: dotnet-app-service
spec:
  selector:
    app: dotnet-app
  ports:
  - protocol: TCP
    port: 80 # Service port
    targetPort: 5000 # Port in the container
  type: LoadBalancer # Use NodePort service type            
